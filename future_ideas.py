'''
#log in function 
def log_in(self,name,password,user):
    if self.user==admin:
        login_as_admin()
    elif self.user==customer:
        login_as_user():
    else:
        return prompet        
        '''
        
        
'''
class Menu():
    it wouid be the interface for users 
    '''
    
'''    
# Input validation in class Item:
        if not isinstance(price, float) or price < 0:
            raise ValueError(f"Price {price} must be a non-negative float.")
        if not isinstance(stock, int) or stock < 0:
            raise ValueError(f"Stock {stock} must be a non-negative integer.")
            '''
            
'''
class Customer:
an other way to create total pay method
def customer_total_pay(self):
        return sum(order.order_cost() for order in self.personal_orders)'''
        


